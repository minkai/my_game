#ifndef MINKAI_ALGORITHM_H
#define MINKAI_ALGORITHM_H

#include <string>
#include <cctype>

using std::string;

bool check_no_same_characters_together( string str );
string cut_right_number( string& str );
string cut_left_number( string& str );

#endif